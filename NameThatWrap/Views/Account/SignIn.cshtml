@using NameThatWrap.Models;
@using NameThatWrap.data;

@model SignInModel

@{
    ViewBag.Title = "SignIn";
    Layout = "../Shared/_Layout.cshtml";
}


    <div class="form" id="content">
        @using (Html.BeginForm("SignIn", "Account"))
        {
            @Html.ValidationSummary();
            <label>Email</label> @Html.TextBoxFor(m => m.Email) @Html.ValidationMessageFor(m => m.Email)
            <label>Password</label> @Html.PasswordFor(m => m.Password) @Html.ValidationMessageFor(m => m.Password)
            <p>@ViewBag.ErrorMessage</p>
            <input class="button" type="submit" value="Sign in" />

        }
    </div><!--end form/content div-->
    <div id="bg">
        <img id="bgimg" src="../../Assets/img/eva.jpg" alt="evacolorway" />
    </div><!--end bg div-->